spring:
  application:
    name: configserver
  cloud:
    config:
      server:
        git:
          uri: https://github.com/****/spring-cloud-config-server-repository
          default-label: main
          username: username
          password: password
          clone-on-start: true
    fail-fast: true
  security:
    user:
      name: name
      password: password